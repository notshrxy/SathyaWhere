/**
 * pages/index.tsx
 * The root index page of the application.
 * Currently acts as a wrapper that renders the main `LandingPage` component.
 */

import LandingPage from './LandingPage';

export default function Home() {
  return <LandingPage />;
}
