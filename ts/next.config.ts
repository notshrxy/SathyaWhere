/**
 * next.config.ts
 * Main configuration file for Next.js. Used to define custom build settings,
 * environment variables, and framework behavior like React Strict Mode.
 */

import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  reactStrictMode: true,
};

export default nextConfig;
