/**
 * postcss.config.mjs
 * PostCSS configuration file. It specifies the plugins used to process
 * CSS, such as the Tailwind CSS PostCSS plugin.
 */

const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
